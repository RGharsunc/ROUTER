var error = 'error12';
var SALTROUNDS = 10;
var socketRoom = 'myRoom';
var PORT = 4000;

module.exports = {

    // exp: exp,
    // app: app,
    // http: http,
    // server: server,
    // // service: service,
    // bodyParser: bodyParser,
    // io: io,
    // path: path,
    // projectDir: projectDir,
    PORT: PORT,
    emitterName: error,
    SALTROUNDS: SALTROUNDS,
    socketRoom: socketRoom,
};